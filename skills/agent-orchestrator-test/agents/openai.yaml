display_name: Agent Orchestrator Test
short_description: Run the pho_phong approval-gated Facebook flow with optional post-approval video creation
default_prompt: Coordinate the separate pho_phong workflow through the orchestrator entry point only; wait for real worker checkpoints and return the exact orchestrator result, never invent interim progress updates while content/media workers are still running.
